#!/bin/bash

ln -fs ~/.dotfiles/ack/.ackrc
ln -fs ~/.dotfiles/bash/.bashrc
ln -fs ~/.dotfiles/gems/.gemrc
ln -fs ~/.dotfiles/git/.gitconfig
ln -fs ~/.dotfiles/.profile
ln -fs ~/.dotfiles/screen/.screenrc
ln -fs ~/.dotfiles/tmux/.tmux.conf
ln -fs ~/.dotfiles/vim/.vim
ln -fs ~/.dotfiles/vim/.vimrc
ln -fs ~/.dotfiles/zsh/.zshrc
